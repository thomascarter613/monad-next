# Tests materialise this fixture into a tempdir, so build artefacts
# should never land here — but if someone runs `monad build` directly
# inside the fixture source while iterating, protect the repo.
app/app
app/.cache/
