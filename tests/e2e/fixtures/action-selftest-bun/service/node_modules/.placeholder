# Sentinel: BunAdapter's install_probe checks `node_modules/` is
# non-empty before deciding whether `bun install --frozen-lockfile`
# needs to run. We don't ship real deps in this selftest fixture, so
# the placeholder satisfies the probe and skips the install step.
